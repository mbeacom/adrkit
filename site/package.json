{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prepare:content": "bun run sync:schema && bun run gen:adr",
    "dev": "bun run prepare:content && astro dev",
    "start": "bun run dev",
    "build": "bun run prepare:content && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync:schema": "bun run scripts/sync-schema.ts",
    "check:schema": "bun run scripts/sync-schema.ts --check",
    "check:rendered": "bun run scripts/check-rendered.ts",
    "check:rendered:self-test": "bun run scripts/check-rendered.ts --self-test",
    "check:rendered:allow-skip": "bun run scripts/check-rendered.ts --allow-skip",
    "gen:adr": "bun run scripts/gen-adr-pages.ts"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.9",
    "@fontsource-variable/anybody": "^5.3.0",
    "@fontsource-variable/atkinson-hyperlegible-next": "^5.3.0",
    "astro": "^7.2.7",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "yaml": "^2.9.0"
  },
  "overrides": {
    "sharp": "^0.35.0"
  }
}
